package gui;

public class Main1 {
    public static void main(String[] args) {
        //JFrame = a GUI window to add components to
        /*JFrame frame = new JFrame();//creates a frame
        frame.setTitle("First JFrame");//sets title of this
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);//exit out of application
        frame.setResizable(false);//prevent this from being resized
        frame.setSize(400,400);//sets x,y dimensions of the frame
        frame.setVisible(true);//make frame visible
        ImageIcon image=new ImageIcon("src\\gui\\logo.jpg");//create an image icon
        frame.setIconImage(image.getImage());//change icon of this
        //frame.getContentPane().setBackground(Color.BLUE);
        //frame.getContentPane().setBackground(new Color(0,0,0));
        frame.getContentPane().setBackground(new Color(0xFFFFFF));*/
        MyFrame myFrame=new MyFrame();

    }
}